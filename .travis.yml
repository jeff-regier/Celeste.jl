language: julia
os:
  - linux
julia:
  - 0.6
notifications:
  email: false

after_success:
  - julia -e 'cd(Pkg.dir("Celeste")); Pkg.add("Coverage"); using Coverage; Codecov.submit(Codecov.process_folder())'

